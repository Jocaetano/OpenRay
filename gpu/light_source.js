
function LightSource(position,  directional,  ambient,  diffuse,  specular) {
	this._position = position;
	this._directional = directional;
	this._ambient = ambient;
	this._diffuse = diffuse;
	this._specular = specular;
}